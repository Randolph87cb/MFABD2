# 项目协作规则

- 排查流程、识别、点击或任务状态机问题时，如果确认存在错误路径或误判样例，必须把对应截图、日志片段或最小复现输入加入易错库，并增加可重复运行的测试或检查脚本；修复完成前应先确认该测试能覆盖本次问题，避免同类问题回归。
- 复刻或排查 PC 客户端流程前，先阅读 `docs/zh_cn/PC流程复刻指南.md`，沿用其中的最短链路、PC 覆盖、易错库、harness 验证和小步提交策略。
